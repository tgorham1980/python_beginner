# structure like is like a list but it cannot be changed. Immutable, cannot be modified.

coordinates = (4, 5) <--- Tuple
coordinates = [(4, 5), (6, 7), (8, 9)]  <--- a list of Tuples


print(coordinates[0])
